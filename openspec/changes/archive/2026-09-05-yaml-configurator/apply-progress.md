# Apply Progress: yaml-configurator

## Completed Tasks

- [x] 1.1 Added semantic-schema RED tests for duplicate IDs, dangling category references, reserved virtual IDs, and valid configurations.
- [x] 1.2 Added shared reserved virtual category constants and Zod semantic refinements with item-level issue paths.
- [x] 1.3 Retained the mounted-config request outcome as valid, missing, invalid, or unavailable without changing the dashboard fallback.
- [x] 1.4 Added schema-backed YAML parsing and deterministic canonical YAML serialization.
- [x] 1.5 Added browser-only clipboard copy and `dashboard.yaml` download boundaries with one safe failure notification.
- [x] 2.1 Added direct lazy `/configure` navigation, root dashboard regression, and route-boundary integration tests.
- [x] 2.2 Added the lazy configurator route and `RouterOutlet` application shell while retaining the global toast host.
- [x] 2.3 Added an in-memory signal-backed draft store with mounted choice, local YAML import, shared validation, dirty state, and no browser persistence.
- [x] 3.1 Added the accessible configurator page entry, settings fields, field-error summary links, and associated validation messaging.
- [x] 3.2 Added generic keyboard-editable category, application, and bookmark controls with intentional post-removal focus.
- [x] 3.3 Corrected Phase 3 visual parity: dashboard-consistent responsive surfaces, hierarchy, form controls, action states, validation presentation, and collection empty states without expanding Phase 4 scope.
- [x] 3.4 Corrected dashboard visual-system integration: reused the shared header and footer, applied the active dashboard background on direct `/configure` navigation, and retained the existing glass editor surfaces, controls, keyboard flow, and AXE contracts.
- [x] 3.5 Corrected the collection composition and application icon editor: collection hosts are block-level with explicit responsive gaps, and applications now expose typed `IconConfig` type/value controls that preserve values, map schema errors, update the draft, and serialize to YAML.
- [x] 3.6 Corrected visual-editor interactions: ID inputs retain keyboard focus, applications select categories from the active draft, move controls are accessible icon buttons with native visible tooltips, and add controls use the dashboard glass-button language.
- [x] 3.7 Corrected bookmark and generated-ID editing: bookmarks expose a labeled URL input wired through the typed draft store; category, application, and bookmark name updates regenerate safe lowercase kebab-case IDs while direct ID edits remain unchanged until the next name update.
- [x] 4.1 Added export controls that validate before canonical browser-only copy/download effects, with page integration coverage for blocked drafts, success notifications, and no server requests.
- [x] 4.2 Documented semantic ID/category constraints and normalized export behavior in the README and both dashboard examples; ran the complete regression suite.

## TDD Cycle Evidence

| Task             | Test file                                                                               | Layer       | Safety net                                  | RED                                                                              | GREEN                                                                                  | Triangulate                                                                                     | Refactor                                                                          |
| ---------------- | --------------------------------------------------------------------------------------- | ----------- | ------------------------------------------- | -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| 1.1–1.2          | `dashboard.models.spec.ts`                                                              | Unit        | N/A — new spec                              | Missing test module failed to resolve                                            | 7 semantic tests passed                                                                | Valid, duplicate, dangling, and reserved cases                                                  | Shared semantic issue helper                                                      |
| 1.3              | `yaml-loader.service.spec.ts`                                                           | Unit        | 7 existing loader tests passed              | New outcome API type check failed                                                | 11 loader tests passed                                                                 | Valid, missing, invalid, unavailable outcomes                                                   | Cached request and readonly signal                                                |
| 1.4              | `yaml-codec.service.spec.ts`                                                            | Unit        | N/A — new service/spec                      | Missing service module failed to resolve                                         | 3 codec tests passed                                                                   | Valid parse, semantic failure, ordered output                                                   | Ordered canonical-object mapper                                                   |
| 1.5              | `config-export.service.spec.ts`                                                         | Unit        | N/A — new service/spec                      | Missing service module failed to resolve                                         | 3 export tests passed                                                                  | Clipboard success/failure and download                                                          | Global URL stub cleanup                                                           |
| 2.1              | `app.routes.spec.ts`                                                                    | Integration | N/A — new spec                              | 3 route tests failed because `/configure` and `/` routes were absent             | 4 route tests passed                                                                   | Lazy child loading, direct router navigation, root dashboard route, and no redirect             | Typed lazy-route result and explicit route-boundary helper                        |
| 2.2              | `app.spec.ts`, `app.routes.spec.ts`                                                     | Integration | 1 existing app test passed                  | Router-shell test failed while the dashboard still composed at root              | 4 route/shell tests passed                                                             | Router outlet, global toast host, eager dashboard root, and lazy configurator boundary          | Prettier-only route and shell cleanup                                             |
| 2.3              | `configurator.store.spec.ts`                                                            | Unit        | N/A — new store/spec                        | Missing store module failed to resolve                                           | 5 store tests passed                                                                   | Mounted valid/unavailable, valid/invalid import, semantic validation, dirty, and no persistence | Cloned draft and shared parse-error mapper                                        |
| 3.1              | `configurator-page.component.spec.ts`                                                   | Integration | 9 existing route/store tests passed         | Missing page and collection-editor modules failed to resolve                     | 2 page tests passed; then 14 focused route/store/page/editor tests passed              | Settings values, associated error summary, keyboard tab order, and AXE scenario                 | Extracted field-error ID mapping and applied targeted Prettier cleanup            |
| 3.2              | `components/collection-editor.component.spec.ts`                                        | Integration | 9 existing route/store tests passed         | Missing collection-editor module failed to resolve                               | 3 collection-editor tests passed; then 14 focused route/store/page/editor tests passed | Category move/remove focus, plus keyboard edits for application category and bookmark name      | Shared generic editor with stable labels and post-mutation nearest-survivor focus |
| 3.3 (corrective) | `components/collection-editor.component.spec.ts`, `configurator-page.component.spec.ts` | Integration | 5 existing focused page/editor tests passed | Empty-state status test failed because no accessible empty-state message existed | 7 focused tests passed                                                                 | Empty category and bookmark collections render distinct actionable status messages              | Applied Prettier after the visual-template refactor; behavior remains green       |
| 3.4 (corrective) | `configurator-page.component.spec.ts`, Chromium `/configure` screenshot | Integration | 3 existing page tests passed before the shell test; 3 tests passed before the direct-background test | Shared-shell test failed because the configurator did not render the dashboard header/footer; direct-background test failed because `#app-background` had no image | 4 page tests and 9 focused page/editor tests passed; Chromium visual comparison passed | Shared shell, theme-toggle tab order, active dark background, editor fields, empty states, and AXE scenario | Reused existing header/footer and mirrored the dashboard background selection at the direct-route boundary |
| 3.5 (corrective) | `components/collection-editor.component.spec.ts`, `configurator-page.component.spec.ts`, `configurator.store.spec.ts`, `yaml-codec.service.spec.ts` | Integration + unit | 4 files, 20 tests passed before the corrective RED tests | Application icon group and nested schema-error association failed because only the incorrect category icon control existed | 4 files, 22 tests passed after the typed application icon group, event wiring, nested error mapping, and block host composition | Name-to-URL type change preserves the value; add/update retains `IconConfig`; URL icon serializes as nested YAML; AXE scenario passes | Added typed collection edit events and canonical nested error keys without changing the dashboard glass surfaces |
| 3.6 (corrective) | `components/collection-editor.component.spec.ts`, `configurator-page.component.spec.ts` | Integration | 2 files, 11 tests passed | Added focus, selector, empty-category, and icon-control tests; RED compilation failed because `categoryOptions` did not exist | 2 files, 17 tests passed after stable index tracking, selector binding, and Heroicons controls | Three collection ID paths; valid and empty category drafts; up/down accessible names and tooltips; page AXE path | Formatted the focused spec after consolidating table-driven interaction coverage |
| 3.7 (corrective) | `configurator.store.spec.ts`, `configurator-page.component.spec.ts`, `components/collection-editor.component.spec.ts` | Unit + Integration | 3 files, 24 focused tests passed | Store/editor RED failed because `normalizeNameToId` was not exported; page RED passed 5 and failed 1 because bookmark URL edits did not call the typed store | 3 files, 32 focused tests passed after the URL binding, typed store wiring, and generated-ID update | Accents, punctuation, repeated separators, unsupported-only names, all three collections, direct ID retention, regenerated collision validation, and typed URL edits | Extracted named normalization patterns; 3 files, 32 focused tests still passed |
| 4.1 | `configurator-page.component.spec.ts` | Integration | 6 existing page tests passed | 3 new page tests failed because Copy YAML and Download YAML controls did not exist | 9 page tests passed after validation-gated canonical serialization and browser export wiring | Invalid copy/download are both blocked; valid copy and download emit separate success notifications and make no `fetch` request | Replaced global Vitest cleanup with descriptor restoration so browser mocks do not disturb other integration specs |
| 4.2 | N/A — documentation-only | Documentation | N/A — no executable production behavior | N/A — documentation changes only | Documentation checked against the shared schema and codec behavior | README and both examples cover unique cross-collection IDs, existing application categories, reserved IDs, and canonical output limits | Triangulation skipped: documentation describes one implemented contract; complete regression was run separately |

## Work Unit Evidence

| Evidence             | Result                                                                                                                                                                                                                                                                                                                     |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused test command | `npx ng test --watch=false --no-coverage --include='src/app/core/models/dashboard.models.spec.ts' --include='src/app/core/services/yaml-codec.service.spec.ts' --include='src/app/core/services/config-export.service.spec.ts' --include='src/app/core/services/yaml-loader.service.spec.ts'` — passed: 4 files, 25 tests. |
| Type check           | `npx tsc --noEmit -p tsconfig.spec.json` — passed.                                                                                                                                                                                                                                                                         |
| Runtime harness      | N/A — no E2E harness; unit tests cover browser APIs via jsdom.                                                                                                                                                                                                                                                             |
| Rollback boundary    | Revert the four core service/model production files and their focused specs; dashboard startup behavior reverts independently.                                                                                                                                                                                             |

## Work Unit 2 Evidence

| Evidence             | Result                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused test command | `npx ng test --watch=false --no-coverage --include='src/app/app.routes.spec.ts' --include='src/app/app.spec.ts' --include='src/app/features/configurator/configurator.store.spec.ts'` — passed: 3 files, 10 tests.                                                                                                                                                        |
| Type check           | `npx tsc --noEmit -p tsconfig.spec.json` — passed.                                                                                                                                                                                                                                                                                                                        |
| Formatting check     | `npx prettier --check "src/app/app.routes.ts" "src/app/app.routes.spec.ts" "src/app/app.ts" "src/app/app.html" "src/app/app.spec.ts" "src/app/features/configurator/configurator.routes.ts" "src/app/features/configurator/configurator.store.ts" "src/app/features/configurator/configurator.store.spec.ts"` — passed after route, shell, and store formatting refactor. |
| Runtime harness      | `RouterTestingHarness` direct `/configure` scenario — passed in `app.routes.spec.ts`; no E2E harness is available.                                                                                                                                                                                                                                                        |
| Rollback boundary    | Revert `app.routes.*`, `app.*`, and `src/app/features/configurator/{configurator.routes.ts,configurator.store.ts}` with their Unit 2 tests. This removes only lazy navigation, the shell, and in-memory draft behavior.                                                                                                                                                   |

## Work Unit 3 Evidence

| Evidence             | Result                                                                                                                                                                                                                                                                                                                                                    |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused test command | `npx ng test --watch=false --no-coverage --include='src/app/app.routes.spec.ts' --include='src/app/features/configurator/configurator.store.spec.ts' --include='src/app/features/configurator/configurator-page.component.spec.ts' --include='src/app/features/configurator/components/collection-editor.component.spec.ts'` — passed: 4 files, 14 tests. |
| Runtime harness      | The focused `RouterTestingHarness` direct `/configure` scenario and Angular Testing Library keyboard/AXE jsdom scenarios passed. No E2E harness is available.                                                                                                                                                                                             |
| Type check           | `npx tsc --noEmit -p tsconfig.spec.json` — passed.                                                                                                                                                                                                                                                                                                        |
| Formatting check     | `npx prettier --check` over the Unit 3 configurator route, store, page, collection-editor, templates, and focused specs — passed.                                                                                                                                                                                                                         |
| Rollback boundary    | Revert `src/app/features/configurator/configurator-page.component.*`, `components/collection-editor.component.*`, and the Unit 3 store/route wiring. This removes only the accessible editor without removing Unit 1–2 validation, routing, and import behavior.                                                                                          |

## Phase 3 Visual Parity Remediation Evidence

| Evidence               | Result                                                                                                                                                                                                                                                                                                                                 |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused test command   | `npx ng test --watch=false --no-coverage --include='src/app/features/configurator/configurator-page.component.spec.ts' --include='src/app/features/configurator/components/collection-editor.component.spec.ts'` — passed: 2 files, 7 tests.                                                                                           |
| Accessibility evidence | The focused page test executed `expectNoAxeViolations` against the rendered configurator and passed. The same run also exercised keyboard tab order, field-error associations, controls, and intentional post-removal focus.                                                                                                           |
| Runtime harness        | Angular Testing Library + jsdom integration scenario — passed: rendered page/editor, keyboard interaction, focus management, and AXE check. No browser/E2E harness exists; a manual visual browser check remains pending and this correction is not declared visually verified.                                                        |
| Type check             | `npx tsc --noEmit -p tsconfig.spec.json` — passed.                                                                                                                                                                                                                                                                                     |
| Formatting check       | `npx prettier --check "src/app/features/configurator/configurator-page.component.html" "src/app/features/configurator/components/collection-editor.component.html" "src/app/features/configurator/components/collection-editor.component.ts" "src/app/features/configurator/components/collection-editor.component.spec.ts"` — passed. |
| Rollback boundary      | Revert the two configurator templates, `collection-editor.component.ts`, and its focused spec. This removes presentation remediation and empty-state messaging without removing Phase 1–3 editing behavior.                                                                                                                            |

## Phase 3.4 Dashboard Visual-System Integration Evidence

| Evidence | Result |
| --- | --- |
| Focused test command | `npx ng test --watch=false --no-coverage --include='src/app/features/configurator/configurator-page.component.spec.ts' --include='src/app/features/configurator/components/collection-editor.component.spec.ts'` — passed: 2 files, 9 tests. |
| Accessibility evidence | `ConfiguratorPageComponent` executed `expectNoAxeViolations` with the shared dashboard header and footer rendered. Keyboard tab order now explicitly covers the shared theme toggle, validation summary, and settings field. |
| Runtime harness | `npx ng serve --host 127.0.0.1 --port 4200` plus `chromium --headless --virtual-time-budget=5000 --window-size=1440,1200 --screenshot=/tmp/getmando-configurator-phase-3-4.png http://127.0.0.1:4200/configure` — passed. Compared with `/tmp/getmando-dashboard-phase-3-4.png`: both routes render the shared header/footer, branded background, translucent bordered surfaces, light foreground hierarchy, and responsive spacing. |
| Type check | `npx tsc --noEmit -p tsconfig.spec.json` — passed. |
| Formatting check | `npx prettier --check` over the configurator page, collection editor templates, and focused specs — passed. |
| Rollback boundary | Revert `configurator-page.component.ts`, `.html`, and `.spec.ts`. This removes only the direct-route background lifecycle and shared dashboard shell composition, leaving Phase 1–3 data editing and Phase 4 work intact. |

## Phase 3.5 Application Icon and Collection Spacing Evidence

| Evidence | Result |
| --- | --- |
| Focused test command | `npx ng test --watch=false --no-coverage --include='src/app/features/configurator/configurator-page.component.spec.ts' --include='src/app/features/configurator/components/collection-editor.component.spec.ts' --include='src/app/features/configurator/configurator.store.spec.ts' --include='src/app/core/services/yaml-codec.service.spec.ts'` — passed: 4 files, 22 tests. |
| Accessibility evidence | The focused page test ran `expectNoAxeViolations` after the application icon error association was added; it passed with the shared shell and keyboard flow. |
| Runtime harness | Chromium on `/configure`: after adding a category and application, the application icon type changed from `initials` to `url` while preserving `NA`, then accepted `https://icons.example.test/new.svg`. Computed collection hosts were `display: block`; desktop gaps were 48 px and 48 px at 1440 px, narrow gaps were 40 px and 40 px at 390 px. Screenshots: `/tmp/getmando-phase-3-5-icon-desktop.png`, `/tmp/getmando-phase-3-5-icon-narrow.png`. |
| Type check | `npx tsc --noEmit -p tsconfig.spec.json` — passed. |
| Formatting check | `npx prettier --check` over the Phase 3.5 page/editor and focused test files — passed. |
| Rollback boundary | Revert `collection-editor.component.{ts,html,spec.ts}`, `configurator-page.component.{ts,html,spec.ts}`, `configurator.store.spec.ts`, and `yaml-codec.service.spec.ts`. This removes only application icon editing, nested icon-error presentation, and explicit collection-host spacing; prior routing, validation, and Phase 4 remain unaffected. |

## Phase 3.6 Visual Editor Corrections Evidence

| Evidence | Result |
| --- | --- |
| Focused test command | `npx ng test --watch=false --no-coverage --include='src/app/features/configurator/configurator-page.component.spec.ts' --include='src/app/features/configurator/components/collection-editor.component.spec.ts'` — passed: 2 files, 17 tests. |
| Type check | `npx tsc --noEmit -p tsconfig.spec.json` — passed. |
| Formatting check | `npx prettier --check "src/app/features/configurator/components/collection-editor.component.ts" "src/app/features/configurator/components/collection-editor.component.html" "src/app/features/configurator/components/collection-editor.component.spec.ts" "src/app/features/configurator/configurator-page.component.html" "openspec/changes/yaml-configurator/tasks.md"` — passed. |
| Runtime harness | `npx ng serve --host 127.0.0.1 --port 4200` with `chromium --headless --no-sandbox --disable-gpu --virtual-time-budget=5000 --dump-dom http://127.0.0.1:4200/configure` — passed. Direct `/configure` loaded the dashboard shell and background, rendered all three glass-style add controls with `heroPlus`, and exposed the safe empty-category state. Angular Testing Library integration covered keyboard focus retention, selection changes, and AXE. |
| Rollback boundary | Revert `src/app/features/configurator/components/collection-editor.component.{ts,html,spec.ts}` and the application `categoryOptions` binding in `configurator-page.component.html`. This removes only Phase 3.6 editor behavior and visual controls without affecting prior Phase 3 icon work or Phase 4. |

## Phase 3.7 Bookmark URL and Generated IDs Evidence

| Evidence | Result |
| --- | --- |
| Focused test command | `npx ng test --watch=false --no-coverage --include='src/app/features/configurator/configurator.store.spec.ts' --include='src/app/features/configurator/configurator-page.component.spec.ts' --include='src/app/features/configurator/components/collection-editor.component.spec.ts'` — passed: 3 files, 32 tests. |
| Type check | `npx tsc --noEmit -p tsconfig.spec.json` — passed. |
| Formatting check | `npx prettier --check "src/app/features/configurator/configurator.store.ts" "src/app/features/configurator/configurator.store.spec.ts" "src/app/features/configurator/configurator-page.component.ts" "src/app/features/configurator/configurator-page.component.spec.ts" "src/app/features/configurator/components/collection-editor.component.html" "src/app/features/configurator/components/collection-editor.component.spec.ts" "openspec/changes/yaml-configurator/tasks.md"` — passed. |
| Runtime harness | `npx ng serve --host 127.0.0.1 --port 4200` with `chromium --headless --no-sandbox --disable-gpu --virtual-time-budget=5000 --dump-dom http://127.0.0.1:4200/configure` — passed. Chromium loaded the direct configurator route, shared shell, active background, and the Categories, Applications, and Bookmarks editor boundaries. Angular Testing Library exercised bookmark URL entry and typed-store wiring; no E2E interaction harness is available. |
| Rollback boundary | Revert `configurator.store.{ts,spec.ts}`, `configurator-page.component.{ts,spec.ts}`, and `components/collection-editor.component.{html,spec.ts}`. This removes only bookmark URL editing and name-driven ID generation, leaving existing schema uniqueness validation and Phase 4 untouched. |

## Work Unit 4 Evidence

| Evidence | Result |
| --- | --- |
| Focused test command | `npx ng test --watch=false --no-coverage --include='src/app/features/configurator/configurator-page.component.spec.ts'` — passed: 1 file, 9 tests. |
| Runtime harness | Angular Testing Library + jsdom page integration exercised invalid export gating, successful Clipboard API copy, Blob `dashboard.yaml` download, success notifications, and no `fetch` call — passed. No E2E harness is available. |
| Type check | `npx tsc --noEmit -p tsconfig.spec.json` — passed. |
| Formatting check | `npx prettier --check "src/app/features/configurator/configurator-page.component.ts" "src/app/features/configurator/configurator-page.component.html" "src/app/features/configurator/configurator-page.component.spec.ts"` — passed. `npm run format:check` remains externally failing on pre-existing `src/app/core/models/dashboard.models.spec.ts`. |
| Full regression | `npx ng test --watch=false` — external failure: 235 passed, 1 failed in pre-existing `src/app/app.routes.spec.ts` because jsdom lacks `window.matchMedia` while directly constructing `ThemeService`; the focused route spec fails identically. This work unit does not edit the route spec, theme service, or test setup. |
| Rollback boundary | Revert `src/app/features/configurator/configurator-page.component.{ts,html,spec.ts}`, `README.md`, `config/dashboard.example.yaml`, and `public/config/dashboard.example.yaml`. This removes only page export integration and its documentation; it leaves prior schema, codec, and browser export boundaries intact. |

## Full-Suite Result

`npx ng test --watch=false` ran 193 tests: 189 passed and 4 failed in the pre-existing `src/testing/check-focused-tests-cli.spec.ts`. The failures expect child-process stdout/stderr that was empty. This is unrelated to this work unit; no unrelated files were modified. Unit 2 did not run the full suite; independent SDD verification owns it.

## Workload

- Delivery: one PR with an explicit accepted `size:exception`; no branch, commit, push, or PR was created by this work unit.
- Work Unit 2 boundary: route regression tests → lazy router shell → isolated in-memory mounted/import/validation draft store. Phase 3 editors and Phase 4 export integration/docs remain untouched.
- Native settlement recorded 372 changed lines for Work Unit 2 against its start candidate (1,394 lifetime changed lines including Work Unit 1). The 1,300-line limit is accepted by the maintainer for the cohesive feature; code and tests were not compressed to fit it.
- Work Unit 3 boundary: accessible page entry/settings/errors → labeled category/application/bookmark controls with keyboard editing and intentional focus. Phase 4 export integration and documentation remain untouched.
- Delivery remains one PR with the maintainer-approved `size:exception`; no Git or GitHub delivery action was performed.
- Corrective Phase 3 visual parity remediation boundary: existing semantic editor → dashboard-consistent visual presentation and accessible empty states. Phase 4 export integration and documentation remain unchecked and untouched. Visual browser verification remains pending because no browser harness was run.
- Corrective Phase 3.4 dashboard visual-system integration boundary: direct `/configure` route → shared dashboard header/footer and active background lifecycle, verified with Chromium against the current `/` dashboard. Phase 4 export integration and documentation remain unchecked and untouched.
- Corrective Phase 3.5 boundary: application `IconConfig` entry and collection-host composition only. The previous terminal objective that incorrectly targeted category icons was settled as failed, then the maintainer-authorized reset acquired the correctly scoped application-icon objective. Phase 4 export integration and documentation remain unchecked and untouched.
- Corrective Phase 3.6 boundary: stable ID input focus, draft-backed category selection, accessible Heroicons move controls, and dashboard-glass add controls only. Delivery remains the maintainer-approved `size:exception`; no Git or GitHub action was performed, and Phase 4 remains unchecked and untouched.
- Corrective Phase 3.7 boundary: bookmark URL field and deterministic name-driven IDs only. The maintainer approved `size:exception`; no commit, branch, PR, or Phase 4/export/documentation work was performed.
- Work Unit 4 boundary: page-level validation-gated canonical export controls and their integration specs → semantic/canonical-output guidance in the README and both examples. Delivery remains the maintainer-approved `size:exception`; no Git or GitHub action was performed.

## Remaining Tasks

- [x] 4.1–4.2 Export integration and alignment

## Remediation Build Evidence

- Added explicit `/configure` entry controls for start-empty, retained mounted YAML load, and local YAML import.
- Completed schema-backed editing coverage for metadata, settings, categories, applications, and bookmarks; removed category icon handling because categories do not support icons.
- Replaced narrow validation mappings with collection-wide error ID/message mapping for categories, applications, and bookmarks.
- Added jsdom `matchMedia` setup so direct route verification remains green.
- Verification: `npx tsc --noEmit -p tsconfig.spec.json` passed.
- Verification: `npx ng test --watch=false --no-coverage` passed: 34 files, 241 tests.
